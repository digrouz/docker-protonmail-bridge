Your ProtonMail Bridge has been deployed.

To access the bridge services within the cluster, use the following service endpoints:
  IMAP: {{ include "protonmail-bridge.fullname" . }}-svc:{{ .Values.service.ports.imap }}
  SMTP: {{ include "protonmail-bridge.fullname" . }}-svc:{{ .Values.service.ports.smtp }}

{{- if .Values.gatewayApi.enabled }}
Gateway API TCPRoutes have been created. Ensure your parentRefs match your configured Gateway.
{{- end }}
